// const jwt = require('jsonwebtoken')

// const admin = (req, res, next) => {
//     const token = req.headers('x-admin-token');

//     if (!token) return res.status(401).send('Access denied, Not an admin')
// }